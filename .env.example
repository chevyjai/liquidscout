# Base RPC endpoint. A paid provider (Alchemy/QuickNode) is strongly
# recommended — the tick fetcher does several multicalls per refresh.
NEXT_PUBLIC_BASE_RPC_URL=https://base-rpc.publicnode.com

# Server-side only (used by scripts/fetchTicks.ts)
BASE_RPC_URL=https://base-rpc.publicnode.com
